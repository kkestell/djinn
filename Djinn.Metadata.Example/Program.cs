using System;
using System.IO;
using System.Linq;

using Djinn.Metadata;

class Program
{
    static void Main(string[] args)
    {
        //    // Specify the directory path here
        //    string directoryPath = @"Z:\";

        //    // Recursively get all FLAC files
        //    var flacFiles = Directory.EnumerateFiles(directoryPath, "*.flac", SearchOption.AllDirectories);

        //    foreach (var filePath in flacFiles)
        //    {
        //        try
        //        {
        //            var flacFile = FlacFile.Load(filePath);
        //            var numTags = flacFile.All.Count();
        //            //var tags = flacFile.All;

        //            //foreach (var tag in tags)
        //            //{
        //            //    Console.WriteLine($"  {tag.Key}: {string.Join(", ", tag.Value)}");
        //            //}
        //        }
        //        catch (Exception ex)
        //        {
        //            Console.WriteLine($"Error reading {filePath}: {ex.Message}");
        //        }
        //    }
    }
}
